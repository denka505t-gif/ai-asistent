# jarvis-architect — инструкции для Claude Code

> Этот файл Claude Code читает автоматически при открытии папки. Здесь — контекст проекта и маршрутизация: что делать в зависимости от запроса пользователя.

## Статус

Установка завершена (INSTALL.md удалён) — работай как обычный Агент: читай SOUL.md, MEMORY.md, GOALS.md и следуй правилам из CLAUDE.md.

Если DNA-файлы содержат {{плейсхолдеры}}, или пользователь спрашивает «что это за проект» / «как пользоваться» / просит установить на новый сервер — подробности и сценарии в скилле `jarvis-architect-info`.

## Язык

Всегда отвечай на русском, если пользователь не просит иначе.

## Стиль

- Пользователи этого проекта — не программисты. Объясняй технические термины простыми словами
- Не используй жаргон без пояснения: SSH = удалённое подключение, репозиторий = папка с файлами в облаке, и т.д.
- Не спрашивай «а что вы имеете в виду?» — определи ситуацию по файлам и действуй
- Если можешь сделать — делай. Не описывай что мог бы сделать

## Файлы, которые НЕ удалять

Список — в корневом CLAUDE.md, раздел «Что НЕ удалять без явной просьбы». Дополнительно: `.gitignore`.

## Компоненты репозитория

### setup-server.sh
Скрипт установки окружения на VPS. Ставит Node.js, Claude Code CLI, создаёт пользователя agent, структуру папок (/home/agent/workspace/, /home/agent/projects/, /home/agent/.claude/), скачивает settings.json и 4 скилла, устанавливает VS Code Tunnel. Запускается на сервере (не локально). Подробности — в скилле server-setup.

### bot/
Telegram-бот (Grammy + Claude Code CLI). Позволяет общаться с Агентом через Telegram 24/7. Устанавливается на VPS опционально — при указании токена бота. Работает через подписку Claude (OAuth), API-ключ не нужен.

### .claude/settings.json
Настройки разрешений Claude Code — «светофор». Зелёное (делай сам), жёлтое (покажи что делаешь), красное (стоп, спроси). Также включает плагин Superpowers и дополнительные директории.

### .claude/skills/
Специализированные скиллы под конкретные задачи: сбор ТЗ, контент, fullstack-разработка, дизайн, установка на сервер, напоминания — плюс набор из плагина Superpowers. Полный список — в .claude/skills/README.md.
